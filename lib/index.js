'use strict';

const base = 'https://github.com/'
const org  = 'runtimeverification/'
const repo = 'kevm-vm-binaries/'
const releases = '/releases/download/'
const version = '0.0.1'

exports.baseUrl = base + org + repo + releases + '/v' + version
